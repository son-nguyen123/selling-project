# Supabase project URL (Settings → API → Project URL)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase anonymous / public key (Settings → API → anon public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...

# Supabase service-role secret key (Settings → API → service_role secret)
# NEVER expose this in client-side code or commit it to source control.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...

# Resend API key for sending order confirmation emails (https://resend.com)
# Get a free key at https://resend.com – required to send download links via email.
RESEND_API_KEY=re_...

# Sender address for order emails (must be a verified domain on Resend)
RESEND_FROM_EMAIL=noreply@yourdomain.com
